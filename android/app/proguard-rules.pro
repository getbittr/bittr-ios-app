# Scaffold defaults. Real rules land with the wallet layer (BIT-6) — ldk-node and
# BDK go through UniFFI/JNA, and reflective/JNI entry points must be kept or the
# release build fails at runtime rather than at compile time.
