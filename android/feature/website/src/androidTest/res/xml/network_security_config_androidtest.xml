<?xml version="1.0" encoding="utf-8"?>
<!--
  Loopback only, for the instrumentation test APK only. See the sibling
  AndroidManifest.xml for why this is needed at all.

  base-config is restated rather than left to the platform default so that the
  intent is legible in one file: everything except loopback keeps the default
  refusal, which is what makes this narrower than usesCleartextTraffic="true".

  Both spellings are listed because LocalTestServer binds InetAddress.getByName
  ("127.0.0.1") and builds its origin from the literal address, while a page or a
  redirect may still name `localhost`; matching is by name, so one does not cover
  the other.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
