# Screenshots

Two different things land under this name. Keep them apart.

**Flow evidence** — what Maestro writes. Each `takeScreenshot` step in
`shared/flows/**` resolves to `shared/docs/screenshots/<flow_name>/<step>.png`
(see `shared/flows/README.md` → *Conventions*). These are test artefacts: they are
regenerated by a run and nobody reviews them as copy. Some committed flow captures
also live under `shared/flows/.maestro/screenshots/`.

**Store listing sets** — curated, and reviewed as public copy before upload. These
are *not* flow output. They are subject to rules that flow evidence is not:

- The Play Store set must be captured with **CHF explicitly selected**. A clean
  device defaults to EUR and produces euro-denominated screenshots without failing
  anything.
- Screenshots are copy: **Tier 1 item 1** in the `perimeter` document (BIT-27). The
  Compliance & Regulatory Officer reviews the set before it is uploaded, and every
  later change to a publicly-used screenshot is Tier 2.

Before capturing a store set, read **`android/docs/play-store-screenshots.md`**.

> No Play Store set exists yet — the Android port has no screen that shows a fiat
> amount (BIT-47).
